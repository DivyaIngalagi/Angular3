import { TtClasDirective } from './tt-clas.directive';

describe('TtClasDirective', () => {
  it('should create an instance', () => {
    const directive = new TtClasDirective();
    expect(directive).toBeTruthy();
  });
});
